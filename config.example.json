{
  "_comment": "复制本文件为 config.json 并修改。adminPassword 用于 Web 管理后台；llm 段用于程序内 AI 解卦（不配也能用，只是退化为只出提示词）。所有字段都可用环境变量覆盖。",
  "adminPassword": "请改成你自己的密码",
  "llm": {
    "_comment": "解卦大模型配置。provider 选 openai（兼容 OpenAI/DeepSeek/Kimi/智谱/通义/Ollama/各类网关）或 anthropic（Claude 原生）。enabled=false 或 apiKey 为空则不启用程序内解卦。",
    "enabled": false,
    "provider": "openai",
    "baseURL": "https://api.openai.com/v1",
    "apiKey": "sk-在此填入你的密钥",
    "model": "gpt-4o-mini",
    "temperature": 0.7,
    "maxTokens": 2048,
    "timeoutSec": 120
  }
}
